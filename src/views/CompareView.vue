<script setup lang="ts">
</script>

<template>
  <div class="page">
    <h1 class="page-title">Compare</h1>
    <p class="page-subtitle">Run the same prompt against multiple configurations side-by-side.</p>
    <div class="placeholder-card">
      <p>Coming soon.</p>
    </div>
  </div>
</template>

<style scoped>
.page-title {
  font-size: 20px;
  font-weight: 600;
  margin-bottom: var(--space-2);
}

.page-subtitle {
  color: var(--muted);
  margin-bottom: var(--space-6);
}

.placeholder-card {
  padding: var(--space-8);
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  text-align: center;
  color: var(--muted);
}
</style>
